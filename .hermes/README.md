# Hermes home

This directory is the repository-local `HERMES_HOME` used by the GitHub bot. Hermes
configuration, sessions, memories, and skills can be persisted here and reviewed
or restored with the rest of the repository.

Do not store API keys in this directory. Configure secrets in GitHub Actions.